# ── AI Cold Outreach CRM — dependencies ─────────────────────────────
# Pin minor versions for reproducible production installs.

# Google Sheets (CRM)
gspread==6.1.2
google-auth==2.33.0

# Config / secrets
python-dotenv==1.0.1

# Website research
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
tldextract==5.1.2

# AI providers (both optional at runtime; installed for switchability)
anthropic==0.34.2
openai==1.51.0
# Pin httpx — newer httpx (>=0.28) drops the `proxies` arg and breaks the
# openai/anthropic client init ("unexpected keyword argument 'proxies'").
httpx==0.27.2

# Retry / resilience
tenacity==9.0.0

# Data / dates
python-dateutil==2.9.0

# Web dashboard (local control panel)
Flask==3.0.3
waitress==3.0.0

# Bulk lead import (CSV / Excel)
pandas==2.2.2
openpyxl==3.1.5

